# Base URL of your Express backend. Leave unset to run the UI fully on mock data.
VITE_API_BASE_URL=http://localhost:5000/api

# Set to "true" to force mock mode even if a backend URL is set (useful for demos/screenshots)
VITE_USE_MOCK=true
